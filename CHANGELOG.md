# 12-06-2026

- 15:02 - Création d'un nouveau jeu d'icônes de navigation haute qualité dans `public/icons/icons-nav-clean`. Les icônes simples ont été recréées en SVG vectoriel afin de supprimer l'effet pixelisé lors de l'agrandissement, tandis que les icônes de panier plus complexes ont été conservées en PNG mais générées en version agrandie et adoucie pour améliorer leur rendu visuel.
- 15:28 - Suppression du dossier `public/icons/icons-nav-clean` après validation visuelle insuffisante des icônes vectorielles générées. La page `/[locale]/home` utilise désormais les icônes PNG originales du dossier `public/icons/icons-nav` pour sa navigation basse, avec un mapping adapté aux icônes disponibles dans le projet.
- 15:31 - Remplacement des icônes de navigation de la page d'accueil `/[locale]` par les fichiers PNG originaux `back.png` et `next.png` issus de `public/icons/icons-nav`, afin d'utiliser le même jeu d'icônes que les autres pages existantes.
- 16:12 - Intégration du modèle 3D `public/models/star.glb` dans la page `/[locale]/home` à la place de l'ancienne zone centrale composée du choix de langue, de l'image des livres et du texte d'appel à l'action. Le rendu Three.js est statique, sans rotation, et son cadrage a été ajusté pour afficher le modèle dans la zone centrale de la page.
- 16:17 - Agrandissement du modèle 3D `star.glb` dans la page `/[locale]/home` avec une échelle doublée et ajout d'une rotation lente autour de son centre géométrique, afin de rendre l'objet plus visible tout en conservant un mouvement maîtrisé.

# 15-06-2026

- 10:08 - Reprise de l'animation du modèle 3D `star.glb` dans la page `/[locale]/home` avec une rotation très lente autour de son centre géométrique, en conservant le centrage full width validé dans la zone centrale.
- 10:08 - Ajout de logs `[DEBUG LOG]` dédiés à la rotation du modèle pour suivre l'angle en degrés et confirmer pendant le rendu que le pivot reste bien centré.
- 10:15 - Ajout d'un sélecteur de langue compact et discret sur la page `/[locale]/home`, avec les drapeaux issus de `public/icons/flags` et un changement réel de locale vers l'URL correspondante via `next-intl`.
- 10:16 - Ajustement du sélecteur de langue de la page `/[locale]/home` pour qu'il puisse s'ouvrir à la fois au survol et au clic, tout en conservant la fermeture au clic extérieur.
- 10:19 - Amélioration de l'ergonomie du sélecteur de langue de la page `/[locale]/home` avec suppression de l'espace entre le bouton et la liste, ainsi qu'un léger délai de fermeture pour éviter qu'il disparaisse trop vite lors du passage de la souris.
- 10:46 - Création de la nouvelle page `/[locale]/menu` en reprenant la composition FileMaker de référence avec en-tête daté, panneau central de consultation en trois colonnes, quatre boutons d'accès rapide, barre d'icônes basse et textes traduits dans les six locales du projet.
- 10:51 - Recalage du layout desktop de la page `/[locale]/menu` avec correction des chevauchements du header, repositionnement des blocs d'introduction, remontée du panneau central, resserrage des boutons intermédiaires et ajustement de la barre basse pour se rapprocher davantage de la capture FileMaker.
- 10:51 - Compression verticale supplémentaire de la page `/[locale]/menu` pour supprimer le grand espace vide entre les boutons intermédiaires et la barre d'icônes basse, avec repositionnement explicite des deux zones sur desktop.
- 15:40 - Création des composants réutilisables `stavnet-header` et `stavnet-footer`, puis intégration du footer unifié dans `/[locale]`, `/[locale]/home` et `/[locale]/menu` avec des boutons de taille homogène et un ordre de navigation plus cohérent, notamment `Quitter` placé en fin de liste.
- 15:49 - Déplacement des composants partagés `stavnet-header` et `stavnet-footer` dans un dossier transversal `src/components/stavnet`, harmonisation du header entre `/[locale]`, `/[locale]/home` et `/[locale]/menu`, agrandissement du badge central, réalignement du bloc titre à droite et correction du chevauchement entre le texte descriptif et le footer sur la page `/[locale]`.
- 15:49 - Ajustement spécifique du footer de la page `/[locale]` en mode compact pour éviter les zones de clic surdimensionnées lorsqu'il n'y a que deux boutons, avec maintien d'un espace visuel propre entre le texte descriptif et la navigation basse.
- 15:49 - Refonte finale du footer de la page `/[locale]` en mode "cover" avec texte descriptif intégré entre `Retour` et `Suite`, puis remontée explicite du bloc central afin qu'il soit visuellement aligné avec les deux boutons sans toucher ni l'image ni la zone de navigation.
- 19:34 - Recalage du header de la page `/[locale]/home` avec élargissement et meilleur alignement du bloc de titre multilingue, ajout d'un positionnement dédié pour le contrôle droit dans `stavnet-header` et descente du sélecteur de langue afin qu'il ne touche plus le contenu du header.
- 19:47 - Correction de l'alignement vertical de la ligne multilingue sous `Literature Database` dans `/[locale]/home` en imposant une hauteur identique et un centrage interne aux libellés arabe et hébreu pour compenser leurs métriques typographiques différentes.
- 19:48 - Descente légère du modèle 3D `star.glb` sur `/[locale]/home` en abaissant le pivot vertical dans `src/components/home/star-model-viewer.tsx`, afin de le positionner un peu plus bas dans la composition de la page.
- 19:52 - Correction du footer de la page `/[locale]/menu` qui n'était pas ancré en bas de l'écran sur desktop : remplacement du positionnement fixe `top-[640px]` par un ancrage `bottom-[2.3vh]` cohérent avec les autres écrans.
- 19:53 - Descente légère du contenu central de la page `/[locale]/menu` sur desktop en abaissant son ancrage principal de `160px` à `180px`, afin de créer un meilleur équilibre visuel entre le header, le panneau central et le footer.
- 19:54 - Descente plus marquée du contenu central de `/[locale]/menu` sur desktop en poussant son ancrage principal à `220px`, afin d'augmenter franchement l'espace sous le header et d'obtenir une composition plus harmonieuse.
- 19:58 - Refonte interactive de la zone centrale de `/[locale]/menu` : les familles `Books`, `Persons` et `Organizations` sont désormais affichées hors de la card bleue, et le sous-menu s'affiche dans la card fixe directement sous la famille active au survol, au focus ou au clic.
- 20:03 - Installation de `gsap` via `pnpm` puis ajout d'animations globales sur les pages du segment `/[locale]` avec transitions d'entrée entre changements de route, apparition animée des headers et footers partagés, animation d'entrée de la zone centrale de `/[locale]/menu` et transitions des familles/sous-menus interactifs.
- 20:06 - Correction de la page `/[locale]` après ajout des animations : le bloc `cover-content` avait une hauteur nulle en desktop, ce qui plaçait hors champ le texte descriptif et les boutons du footer ; le conteneur a été rendu positionné et plein écran pour servir de cadre réel à l'image et au footer animé.

# 16-06-2026

- 15:11 - Audit et correction responsive des nouvelles pages `/[locale]/books` et `/[locale]/search` avec adaptation mobile des hauteurs de champs et boutons, empilement du panneau principal de la fiche livre sur petit écran, sécurisation des tableaux vides par scroll horizontal et ajustement des grilles du footer mobile pour éviter les blocs trop tassés.
- 15:07 - Remontée légère du logo STAVNET dans le header partagé `src/components/stavnet/header.tsx` pour corriger son alignement vertical et éviter qu’il paraisse trop bas.
- 15:05 - Réduction de la taille du logo STAVNET dans le header partagé `src/components/stavnet/header.tsx` pour alléger sa présence visuelle sur les pages qui l’utilisent.
- 15:04 - Suppression du texte `Mickael Pariente` dans la page `/[locale]/books` afin de ne conserver que le repère vertical `BOOK` à gauche, comme demandé dans la composition.
- 15:03 - Recalage de la page `/[locale]/books` sur la composition FileMaker avec restauration du logo du header partagé, suppression du faux doublon dans le contenu, repositionnement du bloc d’identité à gauche, correction robuste du libellé `<Fichier manquant>` selon la locale et ajustement des proportions du panneau gauche, des onglets et des repères verticaux.
- 14:43 - Suppression du logo du header partagé sur `/[locale]/books` afin de ne conserver que le bloc logo intégré à la composition FileMaker de la page, et stabilisation de l’affichage du libellé `missingFile` désormais servi depuis les traductions de la page.
- 14:28 - Reprise de `/[locale]/books` pour rapprocher davantage la page du visuel de référence et ajout d’onglets réellement fonctionnels avec changement de panneau actif pour `Quatrième de couverture`, `Table des matières`, `Extraits`, `Critiques de presse`, `Disponibilité`, `Parution` et `Statistiques`.
- 14:21 - Création de la nouvelle page `/[locale]/books` pour la fiche détaillée d’un livre en reprenant la composition FileMaker avec onglets supérieurs, panneau de résumé latéral, blocs bibliographiques tabulaires, repères verticaux et navigation basse dédiée, avec tous les champs de contenu rendus vides.
- 14:21 - Raccordement de la navigation `Books` vers `/[locale]/books` dans la navbar, le menu principal, le footer partagé et les messages i18n des six langues.
- 11:11 - Remontée légère de la rangée des familles `Ouvrages`, `Personnes` et `Organismes` sur `/[locale]/menu` pour recréer un espace propre avec la card centrale et éviter qu’elle ne la touche visuellement.
- 11:09 - Renforcement de la fermeture des sous-menus sur `/[locale]/menu` en vérifiant le focus réel après blur, afin que les sous-items disparaissent immédiatement dès qu’aucun élément de la zone n’est encore actif au clavier.
- 11:08 - Suppression de l’ouverture par défaut d’une famille sur `/[locale]/menu` : le panneau central reste vide au chargement et n’affiche désormais les sous-items qu’au survol d’une famille ou tant que le focus clavier reste dans la zone active.
- 11:05 - Agrandissement et aération des tuiles de familles sur mobile dans `/[locale]/menu`, avec icônes plus présentes, titre rehaussé et sous-titres moins tassés afin d'améliorer la lisibilité du bloc `Organismes` et des autres entrées.
- 10:03 - Correction du panneau d’aide de `/[locale]/search` pour empêcher le débordement du contenu hors de la carte sur mobile, en laissant la hauteur suivre le contenu hors desktop et en conservant la contrainte de hauteur uniquement sur grand écran.
- 10:02 - Recalage visuel du formulaire de `/[locale]/search` avec champs légèrement agrandis, labels plus respirants et redistribution verticale des blocs internes sur toute la hauteur disponible, afin d’éliminer le grand vide avant le bouton de validation sans perdre l’alignement global.
- 09:59 - Alignement strict des deux panneaux de `/[locale]/search` avec une hauteur desktop identique imposée au bloc d’aide et au bloc formulaire, ainsi qu’une restructuration interne du formulaire pour conserver le bouton de validation en bas sans casser l’équilibre vertical.
- 09:58 - Recomposition de l’écran `/[locale]/search` après revue UI du rendu navigateur : séparation nette entre formulaire et footer, réduction de la hauteur artificielle du panneau d’aide, remontée du bouton de validation au contact direct des champs et allègement visuel de la navigation basse pour éviter l’effet d’écrasement.
- 09:43 - Amélioration du responsive et du clean code sur les pages `/[locale]/persons` et `/[locale]/orgs` avec une présentation en cartes sur mobile, une pagination plus souple, une navbar adaptée aux petits écrans, l’allègement des requêtes de recherche via `useDeferredValue`, ainsi que la suppression des commentaires, logs de debug et `any` résiduels signalés par le lint.
- 09:42 - Ajustement du layout de `/[locale]/search` pour régulariser l’alignement des champs, homogénéiser la grille du formulaire et supprimer la valeur préremplie des inputs afin d’afficher un écran de recherche vide par défaut.
- 09:28 - Création de la nouvelle page `/[locale]/search` en reproduisant l’écran FileMaker de recherche avec header daté, panneau d’aide latéral, formulaire multi-critères, boutons d’action et navigation basse dédiée.
- 09:28 - Raccordement de la navigation existante vers la page de recherche en remplaçant l’icône d’accès de `/[locale]/home` par le bouton `Recherche`, extension des types d’URL partagés et ajout des libellés i18n dans les six langues prises en charge.
- 09:13 - Remplacement de la zone de recherche statique sur `/[locale]/menu` par un vrai champ `search` ouvrant une palette `cmdk`, avec recherche réelle dans les sections, sous-items, accès rapides et entrées de navigation, ouverture au clic sur l'input ou l'icône, ainsi que via les raccourcis `Ctrl+K` et `Cmd+K`.
- 09:13 - Allègement visuel de la zone de recherche sur `/[locale]/menu` avec suppression du texte d'aide et de l'ancienne icône séparée, élargissement du champ, puis remplacement de l'indicateur clavier par un pictogramme `commande` en SVG accompagné de la lettre `K`.
- 09:13 - Correction du pictogramme `commande` dans l'indicateur de raccourci de `/[locale]/menu` avec un SVG en forme de trèfle macOS plus fidèle au symbole `⌘`.
- 09:13 - Ajustement du texte d'introduction gauche sur `/[locale]/menu` pour afficher la phrase complète sur une seule ligne en desktop, sans modifier le contenu traduit.
- 09:13 - Remplacement du fond bleu du panneau central de `/[locale]/menu` par l'image `public/images/home/home-image-banner.png`, avec un voile clair de lisibilité et une hauteur desktop augmentée pour donner plus d'ampleur au sous-menu.
- 09:13 - Allègement du voile appliqué sur l'image de fond du panneau central de `/[locale]/menu` et passage de l'image en affichage non rogné pour mieux voir l'illustration complète.
- 09:13 - Ajustement final du fond illustré du panneau central de `/[locale]/menu` pour qu'il occupe toute la largeur disponible sans rognage latéral, via un affichage plein cadre étiré.
- 09:13 - Suppression des marqueurs rouges devant les sous-items du panneau central de `/[locale]/menu` pour alléger la lecture de la liste.
- 09:13 - Transformation des sous-items du panneau central de `/[locale]/menu` en liens plus lisibles et discrètement explicites, avec curseur, soulignement léger, petit indicateur de direction et navigation vers les sections disponibles.
- 09:13 - Augmentation d'environ 50% de la hauteur desktop du panneau central illustré de `/[locale]/menu`, avec repositionnement des boutons jaunes pour conserver l'équilibre vertical de la composition.
- 09:13 - Réduction du sur-agrandissement du panneau central de `/[locale]/menu` pour revenir à une hauteur desktop plus équilibrée, avec remontée correspondante des boutons d'action.
- 09:13 - Remontée légère de l'ensemble du bloc central de `/[locale]/menu` sur desktop pour le rapprocher du header tout en conservant les espacements internes validés.
- 09:51 - Détourage des fonds clairs des icônes du dossier `public/icons/icons-nav` avec génération de versions PNG transparentes, puis suppression des anciennes sources JPG afin d'uniformiser le jeu d'icônes sans fond dans l'application.
- 09:57 - Uniformisation des icônes de navigation actives dans l'interface en remplaçant les derniers usages de `images/home-menu` par les fichiers du dossier `public/icons/icons-nav` sur `/[locale]/home`, `/[locale]/menu` et `/[locale]/search`.
- 10:00 - Agrandissement léger des icônes de navigation partagées dans `stavnet-footer` et des icônes de familles sur `/[locale]/menu` afin d'améliorer leur présence visuelle sans casser la composition.
